apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: maxpower-frontend-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/james-rotary/maxpower-frontend-config.git
    targetRevision: development
    path: overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: maxpower-frontend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
